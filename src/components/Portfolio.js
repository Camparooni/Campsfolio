import React from 'react';

const Portfolio = () => {
    return (
        <section>
            <h2>Projects</h2>
        {/* Content goes here later */}
        </section>
    );
};

export default Portfolio;